╔═══════════════════════════════════════════════════════════════════════════╗
║                                                                           ║
║           CLAUDE CODE CLOUD → LOCAL REPO SYNC - QUICK GUIDE              ║
║                                                                           ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────────────┐
│ 🚀 THE THREE ESSENTIAL COMMANDS                                           │
└───────────────────────────────────────────────────────────────────────────┘

  1️⃣  git fetch --all              # Download all new branches/commits

  2️⃣  git branch -r                # See what branches are available

  3️⃣  git checkout <branch-name>   # Switch to the branch you want


┌───────────────────────────────────────────────────────────────────────────┐
│ 📖 COMMON SCENARIOS                                                       │
└───────────────────────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ SCENARIO A: Claude Code created a NEW branch                           ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

  $ git fetch origin
  $ git branch -r                    # Find the branch name
  $ git checkout claude/new-branch   # Creates local copy automatically

  ✅ Done! You now have the changes locally.


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ SCENARIO B: Claude Code added commits to EXISTING branch               ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

  $ git checkout <branch-name>       # Switch to the branch
  $ git pull                          # Get latest changes

  ✅ Done! Your local branch is updated.


┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ SCENARIO C: Want to see changes BEFORE pulling                         ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

  $ git fetch origin
  $ git log HEAD..origin/<branch> --oneline   # See commits
  $ git diff HEAD..origin/<branch>            # See code changes
  $ git pull                                  # Pull if satisfied

  ✅ Safe preview before making changes.


┌───────────────────────────────────────────────────────────────────────────┐
│ 🔍 USEFUL INSPECTION COMMANDS                                             │
└───────────────────────────────────────────────────────────────────────────┘

  git status                         # What branch am I on? Any changes?
  git branch -a                      # Show all branches (local + remote)
  git branch -r                      # Show only remote branches
  git log --oneline -10              # Recent commits
  git remote show origin             # Detailed remote info
  git ls-remote --heads origin       # List all remote branches


┌───────────────────────────────────────────────────────────────────────────┐
│ ⚡ ONE-LINER SHORTCUTS                                                    │
└───────────────────────────────────────────────────────────────────────────┘

  # Fetch and checkout in one go
  git fetch origin && git checkout claude/branch-name

  # Update current branch
  git pull

  # See all remote claude branches
  git ls-remote --heads origin | grep claude

  # Clean up deleted remote branches
  git fetch --prune


┌───────────────────────────────────────────────────────────────────────────┐
│ 🎯 TYPICAL WORKFLOW                                                       │
└───────────────────────────────────────────────────────────────────────────┘

  Step 1: You work with Claude Code on cloud ☁️
          ↓
  Step 2: Claude Code creates branch + commits on GitHub
          ↓
  Step 3: You run on your local machine 💻

          $ git fetch --all
          $ git branch -r                    # Find branch name
          $ git checkout claude/xyz-branch   # Get it locally
          ↓
  Step 4: You now have all changes! ✅

  Step 5: Make more changes locally (optional)
          ↓
  Step 6: Push back to remote (optional)

          $ git add .
          $ git commit -m "Your changes"
          $ git push


┌───────────────────────────────────────────────────────────────────────────┐
│ ⚠️  COMMON MISTAKES TO AVOID                                             │
└───────────────────────────────────────────────────────────────────────────┘

  ❌ Running git pull without checking current branch
     ✅ Run git status first to know where you are

  ❌ Forgetting to fetch before checking for new branches
     ✅ Always git fetch first

  ❌ Using git clone again to get new changes
     ✅ Use git fetch + git checkout instead

  ❌ Force pushing (git push -f) without understanding it
     ✅ Use git push --force-with-lease if you must


┌───────────────────────────────────────────────────────────────────────────┐
│ 🆘 TROUBLESHOOTING                                                        │
└───────────────────────────────────────────────────────────────────────────┘

  Problem: "Branch not found"
  Solution: git fetch --all first, then try again

  Problem: "Your branch is behind origin/branch"
  Solution: git pull to update

  Problem: "Diverged branches"
  Solution: git pull --rebase or git pull (and resolve conflicts)

  Problem: "Already on branch but no new changes"
  Solution: git pull to get latest commits


┌───────────────────────────────────────────────────────────────────────────┐
│ 📞 QUICK HELP                                                             │
└───────────────────────────────────────────────────────────────────────────┘

  Where am I?          →  git status
  What's available?    →  git branch -r
  Get new branch       →  git checkout <name>
  Update this branch   →  git pull
  See changes          →  git log


═══════════════════════════════════════════════════════════════════════════

💡 TIP: Bookmark this file! Keep it open in a terminal for quick reference.

📖 For detailed guide, see: GIT-SYNC-CHEATSHEET.md

═══════════════════════════════════════════════════════════════════════════
